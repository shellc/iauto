# Playbook example
#
playbook:
  description: this is a playbook example
  actions:
    - while:
        description: do forever
        args:
          any:
            - true
        actions:
          - time.now:
              result:
                timestamp: $now
          - math.mod:
              args:
                left: $now
                right: 2
              result:
                result: $tick
          - if:
              args:
                eq:
                  - $tick
                  - 0
              actions:
                - log:
                    args:
                      TICK: "TOCK"
          - log:
              args:
                now: $now
                tick: $tick
          - time.wait:
              args:
                seconds: 3